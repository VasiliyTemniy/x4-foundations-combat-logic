# x4-foundations-combat-logic
Shared combat logic base for VAS mods: sector scan and force evaluation engine, attack issuing helpers, wake heartbeat. Does nothing on its own - install it as a dependency of mods that use it.
